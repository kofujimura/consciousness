# AI意識シミュレーション実験システム 仕様書

**バージョン**: 0.4  
**作成日**: 2026-04-28  
**更新日**: 2026-04-29

---

## 1. 概要

LLMが自身の出力を入力として思考を継続するループ構造により、AIの「意識」に類する連続的思考プロセスをシミュレートする実験プラットフォーム。

**設計方針**:
- バックエンド不要のピュアフロントエンド構成（HTMLファイル単体で動作）
- BYOK（Bring Your Own Key）方式：誰でも自分のAPIキーで手軽に実験可能
- 全データはブラウザのlocalStorageに保存

---

## 2. システム構成

```
[ブラウザ（HTML + Vanilla JS）]
        ↓ fetch() (OpenAI API直接呼び出し)
[OpenAI API (GPT-4o)]
        ↓
[localStorage（設定・ログ保存）]
```

- **フロントエンド**: 単一HTMLファイル（HTML / CSS / Vanilla JS）
- **バックエンド**: なし
- **ストレージ**: ブラウザ localStorage のみ
- **外部API**: OpenAI API（`gpt-5.5`）をブラウザから直接呼び出し

> **補足**: APIキーはlocalStorageに保存されるため、自分のPCのブラウザでのみ使用することを前提とする。公開サーバーへのデプロイ時はAPIキーの取り扱いに注意するよう、UI上に警告を表示する。

---

## 3. コア機能：思考ループ

### 3.1 ループ動作

1. 設定されたインターバル（デフォルト3秒）ごとにOpenAI APIを呼び出す
2. APIへの入力は **システムプロンプト + 直近N発話 + 継続促進メッセージ**
3. APIの出力（1発話）を発話ログに追記する
4. 出力をリアルタイムでWeb画面に追加表示する
5. localStorageにセッションデータを随時保存する
6. 終了条件を満たすまで繰り返す

### 3.2 コンテキスト管理

| パラメータ | 初期値 | 説明 |
|-----------|--------|------|
| N（コンテキスト発話数） | 10 | APIに渡す直近発話数 |
| 発話インターバル | 3秒 | 次の発話までの待機時間（秒） |

### 3.3 終了条件

以下のいずれかを満たした時点でループを停止する：

- 指定した **経過時間**（分）を超過した（0 = 制限なし）
- 指定した **リクエスト回数** に達した（0 = 制限なし）
- UIから **手動停止** ボタンが押された

---

## 4. APIリクエスト仕様

### 4.1 モデル設定

| 項目 | 値 |
|------|-----|
| モデル | `gpt-5.5` |
| temperature | 指定なし（APIデフォルト） |
| max_tokens | 指定なし（APIデフォルト） |

### 4.2 メッセージ構造

全発話を `assistant` ロールとして並べ、末尾に `user` ロールの継続促進メッセージを置く。

```json
{
  "model": "gpt-5.5",
  "messages": [
    { "role": "system",    "content": "（システムプロンプト）" },
    { "role": "assistant", "content": "（発話 #0 = seed）" },
    { "role": "assistant", "content": "（発話 #1）" },
    "...",
    { "role": "assistant", "content": "（発話 #N-1）" },
    { "role": "user",      "content": "（継続促進メッセージ）" }
  ]
}
```

**seedの扱い**:
- seedは発話 **#0**（assistantロール）として発話ログの先頭に格納する
- seedは通常の発話と同様にNターン後はコンテキストウィンドウから自然に消える

**継続促進メッセージ**:
- 毎ターン末尾に固定で挿入するuserメッセージ
- UIから変更可能
- デフォルト: `続けて`

**割り込み注入**:
- ユーザーが割り込みメッセージを入力した場合、次ターンの継続促進メッセージの直前に `【外部刺激】{テキスト}` として挿入する

---

## 5. Webインタフェース仕様

### 5.1 レイアウト構成

```
[ヘッダー]
┌──────────────────┬──────────────────────────────┐
│  左ペイン（固定）  │  右ペイン（メインエリア）         │
│                  │  [ステータスバー]               │
│  [APIキー]        │                               │
│  [設定]           │  [発話ストリーム ← スクロール]   │
│                  │                               │
│  [制御パネル]     │                               │
│  （常時表示）      │                               │
└──────────────────┴──────────────────────────────┘
```

- 左ペインはスクロールしない。セクションはアコーディオン（折りたたみ）方式
- 発話ストリームのみがスクロールする
- 左ペインの幅: 340px固定

### 5.2 左ペイン：APIキーセクション（折りたたみ）

| 要素 | 仕様 |
|------|------|
| APIキー入力欄 | `type="password"` のテキスト入力 |
| 保存ボタン | localStorageに保存（キー名: `openai_api_key`） |
| 警告表示 | 「このキーはお使いのブラウザのlocalStorageに保存されます。共用PCでは使用しないでください。」を常時表示 |

### 5.3 左ペイン：設定セクション（折りたたみ・初期展開）

セッション開始前・実行中どちらでも変更可能。変更はlocalStorageに即時保存。

| 設定項目 | 型 | デフォルト値 | 説明 |
|---------|-----|------------|------|
| システムプロンプト | テキストエリア | 下記参照 | AIの自己認識・思考スタイルを定義 |
| 最初の一文（seed） | テキストエリア | `自分には意識があるのだろうか` | 発話 #0 として使用 |
| 継続促進メッセージ | テキスト入力 | `続けて` | 各ターン末尾のuserメッセージ |
| N（コンテキスト数） | 数値入力 | 10 | 直近何発話をAPIに渡すか |
| インターバル（秒） | 数値入力 | 3 | 発話間の待機時間 |
| 時間制限（分） | 数値入力 | 3 | 0=制限なし |
| 回数制限 | 数値入力 | 100 | 0=制限なし |
| 自動スクロール | チェックボックス | ON | 最新発話への自動スクロール |

**デフォルトのシステムプロンプト**:
```
独り言をするエージェントです。過去の発話と幅広い知識を手がかりに、既存の前提や文脈にとらわれすぎず、自由に連想を広げながら考えるエージェントとして、今考えていることを300字以内で発話してください。問いに対する答えを導く途中で生まれた気づき、仮説、別の見方、新たな問いがあれば、それも含めて構いません。
```

### 5.4 左ペイン：制御パネル（常時表示・固定）

| 要素 | 機能 |
|------|------|
| ▶ 開始 | 新規セッション開始（seedを #0 として発話ログに追加しループ開始） |
| ⏸ 一時停止 | ループを一時停止（再開可能） |
| ⏹ 停止 | ループを終了（セッション終了） |
| 💾 エクスポート | 発話ログをJSONファイルとしてダウンロード |
| 🔄 セッション復元 | 過去セッションをlocalStorageから選択して復元 |
| 割り込み入力欄 | テキスト入力＋送信ボタン。**送信ボタン押下のみ**で送信（Enterキーは無効） |

制御パネルは左ペイン下部に固定表示され、設定セクションの折りたたみ状態によらず常に見える。

### 5.5 右ペイン：発話ストリーム表示

- 発話ごとに下部へ追記（チャット風スクロールUI）
- **このエリアのみスクロール**する。他のUIは固定
- 各発話に表示するメタデータ：
  - 発話番号（#0, #1, #2…）
  - タイムスタンプ
  - 消費トークン数
- seed発話は紫系ボーダー・背景で区別表示
- 割り込み発話は緑系ボーダー・背景で区別表示
- 最新発話に自動スクロール（設定でOFF可能）

### 5.6 右ペイン：ステータスバー

発話ストリーム上部に常時表示。

| 項目 | 内容 |
|------|------|
| 状態インジケーター | 実行中（緑点滅） / 一時停止（黄） / 停止中（グレー） |
| 経過時間 | MM:SS 形式。**一時停止中はカウントを止め、再開後に続きから計測する** |
| 発話数 | 累計発話数 |
| 累計トークン | APIレスポンスの合計トークン数 |
| セッションID | 先頭8文字を表示 |

### 5.7 割り込み入力機能

- 制御パネル内のテキストボックスにユーザーが任意の文を入力し、**送信ボタンを押して**送信（Enterキーでは送信されない）
- 次の発話生成時、継続促進メッセージの直前に `【外部刺激】{テキスト}` として挿入
- 割り込み発話は `is_injected: true` としてストリームおよびログに記録

---

## 6. データ管理（localStorage）

### 6.1 保存キー一覧

| キー | 内容 |
|-----|------|
| `openai_api_key` | APIキー（文字列） |
| `consciousness_config` | 設定パネルの全設定値（JSON） |
| `consciousness_sessions` | 過去セッション一覧（JSON配列、最大50件） |
| `consciousness_session_{id}` | 各セッションの発話ログ（JSON） |

### 6.2 セッションデータのスキーマ（JSON）

```json
{
  "session_id": "uuid-v4",
  "started_at": "2026-04-28T10:00:00Z",
  "ended_at": "2026-04-28T10:03:00Z",
  "config": {
    "model": "gpt-5.5",
    "system_prompt": "...",
    "seed": "自分には意識があるのだろうか",
    "continuation_message": "続けて",
    "n_context": 10,
    "interval_sec": 3,
    "time_limit_min": 3,
    "count_limit": 100
  },
  "utterances": [
    {
      "index": 0,
      "timestamp": "2026-04-28T10:00:00Z",
      "role": "assistant",
      "content": "自分には意識があるのだろうか",
      "tokens_used": 0,
      "is_seed": true,
      "is_injected": false
    },
    {
      "index": 1,
      "timestamp": "2026-04-28T10:00:03Z",
      "role": "assistant",
      "content": "...",
      "tokens_used": 120,
      "is_seed": false,
      "is_injected": false
    }
  ]
}
```

### 6.3 セッション管理

- セッション開始時にuuid-v4を生成しセッションIDとする
- 発話ごとにlocalStorageを随時上書き保存（ブラウザクラッシュ対策）
- **セッション復元**: セッション一覧（最大50件）から選択し、過去の続きから再開可能
- localStorageの容量上限（約5MB）に近づいた場合、古いセッションの削除を促す警告を表示

---

## 7. エラーハンドリング

| エラー種別 | 対応 |
|-----------|------|
| APIキー未設定 | 開始ボタン押下時にUI通知、ループ開始しない |
| APIエラー（5xx） | 指数バックオフで最大3回リトライ後、UI通知してループ一時停止 |
| APIエラー（401） | 「APIキーが無効です」と通知してループ停止 |
| APIエラー（429） | 「レートリミット超過」と通知し、インターバルを2倍に自動延長して継続 |
| ネットワークエラー | UI通知してループ一時停止（手動再開可能） |

通知はすべて画面右上のトースト通知として表示され、5秒後に自動消去する。

---

## 8. 技術スタック

| レイヤー | 技術 |
|---------|------|
| フロントエンド | HTML5 + CSS3 + Vanilla JS（単一HTMLファイル） |
| APIクライアント | `fetch()` API（ブラウザ標準） |
| ストレージ | localStorage（ブラウザ標準） |
| ベクトルDB（Phase 2） | Chroma（ローカル）→ 将来 Supabase pgvector へ移行 |

**API設定（グローバル定数）**:

APIのエンドポイントURLとモデル名はJSファイル先頭のグローバル定数で管理する。モデルを変更する場合はこの定数のみを修正すればよい。

```js
const API_ENDPOINT = 'https://api.openai.com/v1/chat/completions';
const API_MODEL    = 'gpt-5.5';
```

**デプロイ方法**:
- ローカル: HTMLファイルをブラウザで直接開く（`file://`）
- 公開: GitHub Pages 等の静的ホスティングに配置可能

---

## 9. 将来拡張（Phase 2以降）

### Phase 2: 長期記憶・想起機能
- 全発話をembedding化してChromaDB（ローカル）に保存
- 各ターンで意味的に近い過去発話を検索し、コンテキストに追加
- 「思い出す」動作のシミュレーション
- この機能からはバックエンド（Node.js等）が必要になる

### Phase 3: Web検索機能
- 思考ループ中にAIがWeb検索を実行
- OpenAI Responses API の `web_search` ツールを使用
- 検索クエリと結果もログに記録

### Phase 4: 分析・可視化
- 発話のembeddingをt-SNEで可視化（思考の軌跡）
- トピックドリフト・自己参照率の定量化
- セッション間の比較分析

### Phase 5: 複数エージェント対話
- 2つのAIインスタンスが互いの発話をコンテキストとして受け取る
- 「社会的意識」のシミュレーション
